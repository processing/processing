<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>translate()</name>

<category>Transform</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>translate_.gif</image>
<code>
translate(30, 20);
rect(0, 0, 55, 55);
</code>
</example>


<example>
<image>translate_3.gif</image>
<code>
translate(30, 20);
rect(0, 0, 55, 55);
translate(14, 14);
rect(0, 0, 55, 55);
</code>
</example>

<description>
Specifies an amount to displace objects within the display window. The <b>x</b> parameter specifies left/right translation and the <b>y</b> parameter specifies up/down translation. Transformations apply to everything that happens after and subsequent calls to the function accumulates the effect. For example, calling <b>translate(50, 0)</b> and then <b>translate(20, 0)</b> is the same as <b>translate(70, 0)</b>. If <b>translate()</b> is called within <b>draw()</b>, the transformation is reset when the loop begins again. Further control over applying the transformation is acheived with <b>pushMatrix()</b> and <b>popMatrix()</b>.
</description>

<syntax>
translate(<c>x</c>, <c>y</c>);
</syntax>

<parameter>
<label>x</label>
<description>int: left/right translation</description>
</parameter>

<parameter>
<label>y</label>
<description>int: up/down translation</description>
</parameter>

<returns>None</returns>

<related>
rotate() 
rotateX() 
rotateY() 
rotateZ() 
scale() 
pushMatrix() 
popMatrix() 
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>


</root>
