<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>nfs()</name>

<category>Data</category>

<subcategory>String Functions</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
int a=200, b=-40, c=90; 
String sa = nfs(a, 10); 
println(sa); // prints " 0000000200" 
String sb = nfs(b, 5); 
println(sb); // prints "-00040" 
String sc = nfs(c, 3); 
println(sc); // prints " 090" 
</code>
</example>

<description>
Utility function for formatting numbers into strings. Similar to <b>nf()</b> but leaves a blank space in front of positive numbers so they align with negative numbers in spite of the minus symbol. The value for the <b>digits</b> parameter should always be a positive integer.
</description>

<syntax>
nfs(<c>intValue</c>, <c>digits</c>)
</syntax>

<parameter>
<label>intValue</label>
<description>int: the number to format</description>
</parameter>

<parameter>
<label>digits</label>
<description>int: number of digits to pad with zeroes</description>
</parameter>

<returns>String or String[]</returns>

<related>
nf()
nfp()
nfc()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>


<level>Extended</level>





</root>
