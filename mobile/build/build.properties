# the version of mobile processing
mobile.version=0006
# the version of processing the PDE is based on
processing.version=0124
# the svn executable
svn.executable=svn
# the tag to use when checking out processing
svn.tag.processing=processing-${processing.version}

