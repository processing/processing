 #!/bin/sh
 
APPDIR="$(dirname -- "${0}")"
 
for LIB in \
    java/lib/rt.jar \
    java/lib/tools.jar \
    lib \
    lib/build \
    lib/*.jar \
    ;
do
    CLASSPATH="${CLASSPATH}:${APPDIR}/${LIB}"
done
export CLASSPATH

${APPDIR}/java/bin/java processing.app.Base
