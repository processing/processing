<?xml version="1.0" encoding="ISO-8859-9" standalone="yes"?>
<root>
<name>cursor()</name>

<category>Ortam</category>

<subcategory></subcategory>

<usage>Web &amp; Uygulama</usage>

<example>
<image>cursor_.jar</image>
<code>
// Move the mouse left and right across the image
// to see the cursor change from a cross to a hand

void loop() 
{
  if(mouseX &lt; 50) {
    cursor(CROSS);
  } else {
    cursor(HAND);
  }
}
</code>
</example>

<description>
İmleçi önceden tanımlanmış bir sembole veya görsele çevirir, veya görünmez ise görünür olarak tanımlanmasını sağlar. Eğer bir görseli imleç yapıyorsanız, boyutların 16x16 veya 32x32 olması gereklidir. Eğer web için bir program yapıyorsanız, imleç için görsel yüklemek mümkün değildir. <b>x</b> ve <b>y</b> parametreleri için değerler görselin boyutlarından az olmalıdır.
</description>

<syntax>
cursor()
cursor(<c>MODE</c>)
cursor(<c>resim</c>, <c>x</c>, <c>y</c>)
</syntax>

<parameter>
<label>MODE</label>
<description>ARROW, CROSS, HAND, MOVE, TEXT, WAIT</description>
</parameter>

<parameter>
<label>resim</label>
<description>BImage: BImage türünde herhangi bir değişken</description>
</parameter>

<parameter>
<label>x</label>
<description>int: imleçin yatay aktif spotu</description>
</parameter>

<parameter>
<label>y</label>
<description>int: imleçin dikey aktif spotu</description>
</parameter>

<returns>Yok</returns>

<related>
noCursor()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Bagel</partof>








</root>