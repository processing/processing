<?xml version="1.0" encoding="ISO-8859-9" standalone="yes"?>
<root>
<name>month()</name>

<category>Girdi/Çıktı</category>

<subcategory>Zaman &amp; Tarih</subcategory>

<usage>Web &amp; Uygulama</usage>

<example>
<image>month_.jar</image>
<code>
void setup() {
  BFont metaBold;
  metaBold = loadFont("fonts/Meta-Bold.vlw.gz"); 
  setFont(metaBold, 44); 
  hint(SMOOTH_IMAGES); 
}

void loop() {
  int d = day();    // Values from 1 - 31
  int m = month();  // Values from 1 - 12
  int y = year();   // 2003, 2004, 2005, etc.
  String s = String.valueOf(d);
  text(s, 10, 28);
  s = String.valueOf(m);
  text(s, 10, 56); 
  s = String.valueOf(y);
  text(s, 10, 84);
}
</code>
</example>

<description>
Processing bilgisayarınızdaki saat ile iletişim kurar. <b>month()</b> fonksiyonu içinde bulunulan ayı 0 ile 12 arasında gösterir.
</description>

<syntax>
month()
</syntax>

<parameter>
<label></label>
<description></description>
</parameter>

<returns>int</returns>

<related>
millis() 
second()
minute()
hour()
day()
month()
year()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Bagel</partof>








</root>