<?xml version="1.0" encoding="ISO-8859-9" standalone="yes"?>
<root>
<name>{} (kıvrımlı parantezler)</name>

<category>Yapı</category>

<subcategory></subcategory>

<usage>Web &amp; Uygulama</usage>

<example>
<image></image>
<code>
int[] a = { 5, 20, 25, 45, 70 };

void setup() {
  size(100, 100);
}

void loop() {
  for(int i=0; i&lt;a.length; i++) {
    line(0, a[i], 50, a[i]);
  }
}
</code>
</example>

<description>
<b>for()</b> ve <b>if()</b> gibi fonksiyon ve ifade bloklarının başlangıcını ve sonunu belirler. Kıvrımlı parantezler aynı zamanda dizi tanımlamalarında ilk değerleri belirlemek için kullanılır.
</description>

<syntax>
{ <c>anlatımlar</c> }
{ <c>ele0</c>, ..., <c>eleN</c> }
</syntax>

<parameter>
<label>anlatımlar</label>
<description>herhangi bir geçerli anlatım</description>
</parameter>

<parameter>
<label>ele0 ... eleN</label>
<description>virgüllerle ayrılmış öğe listesi</description>
</parameter>

<returns></returns>

<related>
() (parentheses)
</related>

<availability>1.0</availability>

<type>Operator</type>

<partof>PDE</partof>








</root>