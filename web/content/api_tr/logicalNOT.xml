<?xml version="1.0" encoding="ISO-8859-9" standalone="yes"?>
<root>
<name>! (mantıksal DEĞİL)</name>

<category>Control</category>

<subcategory>Logical Operators</subcategory>

<usage>Web &amp; Uygulama</usage>

<example>
<image>logicalNOT.gif</image>
<code>
boolean a = false; 
if (!a) { 
  rect(30, 20, 50, 50); 
} 
a = true; 
if (a) { 
  line(20, 10, 90, 80); 
  line(20, 80, 90, 10); 
} 
</code>
</example>

<description>
Bir <b>ifade</b>nin Boolean değerini tersine çevirir. Eğer ifade yanlış (<b>false</b>) ise doğru döner, doğru (<b>true</b>) ise yanlış döner. Eğer <b>(a>b)</b> ifadesi doğru olarak değerlendirilirse, <b>!(a>b)</b> yanlış olarak değerlendirilir.
</description>

<syntax>
!<c>ifade</c>
</syntax>

<parameter>
<label>ifade</label>
<description>herhangi bir geçerli ifade</description>
</parameter>

<returns></returns>

<related>
|| (logical OR)
&amp;&amp; (logical AND)
if()
</related>

<availability>1.0</availability>

<type>Operator</type>

<partof>PDE</partof>








</root>