<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>framerate</name>

<category>Environment</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
void setup() {
  framerate(30);
}

void draw() {
  line(0, 0, width, height);
  println(framerate);
}
</code>
</example>

<description>
The system variable <b>framerate</b> contains the approximate framerate of the software as it executes. The initial value is 10 fps and is updated with each frame. The value is averaged (integrated) over several frames. As such, this value won't be valid until after 5-10 frames.
</description>

<syntax>
framerate
</syntax>

<parameter>
<label></label>
<description></description>
</parameter>

<returns></returns>

<related>
framerate()
</related>

<availability>1.0</availability>

<type>System variable</type>

<partof>Core</partof>








</root>
