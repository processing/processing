<?xml version="1.0" encoding="UTF-8" standalone="yes"?>

<root>

<name>splice()</name>

<category>Data</category>

<subcategory>Array Functions</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
String[] sa1 = { "OH", "NY", "CA" };
sa1 = splice(sa1, "KY", 1);
println(sa1);  // Prints OH, KY, NY, CA
println();

String[] sa2 = { "VA", "CO", "IL" };
sa1 = splice(sa1, sa2, 2);
println(sa1);  // Prints OH, KY, VA, CO, IL, NY, CA
</code>
</example>

<description>
Inserts a value or array of values into an existing array. The first two parameters must be of the same datatype. The <b>array</b> parameter defines the array which will be modified and the second parameter defines the data which will be inserted.
<br/> <br/>
When using an array of objects, the data returned from the function must be cast to the object array's data type. For example: <em>SomeClass[] items = (SomeClass[]) splice(array1, array2, index)</em>.
</description>

<syntax>
splice(<c>array</c>, <c>value</c>, <c>index</c>)
splice(<c>array</c>, <c>array2</c>, <c>index</c>)
</syntax>

<parameter>
<label>array</label>
<description>boolean[], byte[], char[], int[], float[], String[], or an array of objects</description>
</parameter>

<parameter>
<label>value</label>
<description>boolean, byte, char, int, float, String, or other Object: value to be spliced in</description>
</parameter>

<parameter>
<label>array2</label>
<description>boolean[], byte[], char[], int[], float[], String[], or an array of objects to be spliced in</description>
</parameter>

<parameter>
<label>index</label>
<description>int: position in the array from which to insert data</description>
</parameter>

<returns>Array (the same datatype as the input)</returns>

<related>
contract()
subset()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>

</root>
