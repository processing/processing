<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>size()</name>

<category>Structure</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
void setup() {
  size(320, 240);
  background(153);
}

void draw() {
  line(0, 0, width, height);
}
</code>
</example>

<example>
<image></image>
<code>
void setup() {
  size(320, 240, P3D);
  background(153);
}

void draw() {
  line(0, 0, 0, width, height, -200);
}
</code>
</example>

<example>
<image></image>
<code>
import processing.opengl.*;

void setup() {
  size(screen.width, screen.height, OPENGL);
  background(153);
}

void draw() {
  line(0, 0, 0, width, height, -200);
}
</code>
</example>

<description>
Defines the dimension of the display window in units of pixels. The <b>size()</b> function should be the first line in <b>setup()</b>. If <b>size()</b> is not called, the default size of the window is 100x100 pixels. The system variables <b>width</b> and <b>height</b> are set by the parameters passed to the <b>size()</b> function. Using variables as the parameters to <b>size()</b> is strongly discouraged and can create problems. Use the <b>width</b> and <b>height</b> variables if you need to know the dimensions of the display window within your program. <br /><br />The MODE parameters selects which rendering engine to use. For example, if you will be drawing 3D shapes for the web use <b>P3D</b>, if you want to export a program with OpenGL graphics accelleration use <b>OPENGL</b>. A brief description of the 4 different renderers follows:<br /><br />P2D (Processing 2D) - 2D renderer supporting Java 1.1 export (NOT CURRENTLY IMPLEMENTED)<br /><br />P3D (Processing 3D) - Fast 3D renderer for the web<br /><br />JAVA2D - The default renderer, higher quality in general, but slower than P2D<br /><br />OPENGL - Interface with OpenGL hardware accelleration to increase speed if an OpenGL graphics card is installed.<br />
</description>

<syntax>
size(<c>width</c>, <c>height</c>)
size(<c>width</c>, <c>height</c>, <c>MODE</c>)
</syntax>

<parameter>
<label>width</label>
<description>int: width of the display window in units of pixels</description>
</parameter>

<parameter>
<label>height</label>
<description>int: height of the display window in units of pixels</description>
</parameter>

<parameter>
<label>MODE</label>
<description>Either P2D, P3D, JAVA2D, or OPENGL</description>
</parameter>

<returns>None</returns>

<related></related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
