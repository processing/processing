<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>getChild()</name>

<category>XML</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
import processing.xml.*;

// The following short XML file called "sites.xml" is parsed 
// in the code below. It must be in the project's "data" directory
// <?xml version="1.0"?>
// <websites>
//   <site id="0" url="processing.org">Processing</site>
//   <site id="1" url="mobile.processing.org">Processing Mobile</site>
// </websites>

XMLElement xml;

void setup() {
  size(200, 200);
  xml = new XMLElement("sites.xml", this);
  XMLElement kid = xml.getChild(0);
  String site = kid.getContent();
  println(site);    
}
</code>
</example>

<description>
Returns the child XMLElement as specified by the <b>which</b> parameter. The value of the <b>which</b> parameter must be less than the total number of children to avoid going out of the array storing the child elements.
</description>

<syntax>
<c>xml</c>.getChild(<c>which</c>)
</syntax>

<parameter>
<label>xml</label>
<description>any variable of type XML</description>
</parameter>

<parameter>
<label>which</label>
<description>int: the element</description>
</parameter>

<returns>XMLElement</returns>

<related>
getChildCount()
getChildren()
</related>

<availability>1.0</availability>

<type>Method</type>

<partof>Library</partof>


</root>
