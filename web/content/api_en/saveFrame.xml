<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>saveFrame()</name>

<category>Output</category>

<subcategory>Image</subcategory>

<usage>Application</usage>

<example>
<image></image>
<code>
int x = 0;
void draw()
{
  background(204);
  if(x &lt; 100) {
    line(x, 0, x, 100);
    // Saves each frame as screen-0000.tif, screen-0001, etc.
    saveFrame(); 
    x = x + 1;
  }
}
</code>
</example>

<example>
<image></image>
<code>
int x = 0;
void draw()
{
  background(204);
  if(x &lt; 100) {
    line(x, 0, x, 100);
    // Saves each frame as line-0000.tif, line-0001, etc.
    saveFrame("line-####.tif"); 
    x = x + 1;
  }
}
</code>
</example>

<description>
Saves a numbered sequence of images. If <b>saveFrame()</b> is called without parameters, it will have the files as screen-0000.tif, screen-0001.tif, etc. It is possible to specify the name of the sequence with the <b>filename</b> parameter and make the choice of saving TIFF or TARGA files with the <b>ext</b> parameter. These image sequences can be loaded into programs such as Apple's QuickTime software and made into movies. These files are saved to the sketch's folder, which may be opened by selecting "Show sketch folder" from the "Sketch" menu.
</description>

<syntax>
saveFrame()
saveFrame("<c>filename</c>-####.<c>ext</c>")
</syntax>

<parameter>
<label>filename</label>
<description>String: any sequence of letters and numbers</description>
</parameter>

<parameter>
<label>ext</label>
<description>either "tif" or "tga"</description>
</parameter>

<returns>None</returns>

<related>
save()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
