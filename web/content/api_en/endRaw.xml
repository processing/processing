<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>endRaw()</name>

<category>Output</category>

<subcategory>Files</subcategory>

<usage>Application</usage>

<example>
<image></image>
<code>
import processing.pdf.*;

void setup() {
  size(400, 400);
  beginRaw(PDF, "raw.pdf");
}

void draw() {
  line(pmouseX, pmouseY, mouseX, mouseY);
}

void keyPressed() {
  if (key == ' ') {
    endRaw();
    exit();
  }
}
</code>
</example>

<description>
Complement to beginRaw(), see the beginRaw() reference for details.
</description>

<syntax>
endRaw()
</syntax>

<returns></returns>

<related>
beginRaw()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>


</root>
