<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>PrintWriter</name>

<category>Output</category>

<subcategory>Files</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
PrintWriter output;

void setup() {
  output = new writer("positions.txt"); // Create a new file in the sketch directory
}

void draw() {
  point(mouseX, mouseY);
  output.println(mouseX; // Write the coordinate to the file
}

void keyPressed() {
  output.flush(); // Writes the remaining data to the file
  output.close(); // Finishes the file
  exit(); // Stops the program
}

</code>
</example>

<description>
Allows characters to print to a text-output stream. A new PrintWriter object is created with the <b>writer()</b> function. For the file to be made correctly, it should be flushed and must be closed with its <b>flush()</b> and <b>close()</b> methods (see above example). 
</description>

<syntax>
</syntax>

<parameters>
</parameters>

<field>
<fname></fname>
<fdescription></fdescription>
</field>

<method>
<mname>flush()</mname>
<mdescription>Flushes the stream</mdescription>
</method>
<method>
<mname>close()</mname>
<mdescription>Closes the stream</mdescription>
</method>

<constructor>
</constructor>

<cparameter>
<clabel></clabel>
<cdescription></cdescription>
</cparameter>

<returns></returns>

<related>
writer()
</related>

<availability>1.0</availability>

<type>Object</type>

<partof>Core</partof>


</root>
