<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>PFont</name>

<category>Typography</category>

<subcategory>PFont</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>PFont.gif</image>
<code>
PFont font;
// The font must be located in the sketch's 
// "data" directory to load successfully
font = loadFont("FFScala-32.vlw"); 
textFont(font, 32); 
text("word", 15, 50); 
</code>
</example>

<description>
PFont is the font class for Processing. The <b>loadFont()</b> function constructs a new font and <b>textFont()</b> makes a font active. The <b>list()</b> method creates a list of the fonts installed on the computer, which is useful information to use with the <c>createFont()</c> function for dynamically converting fonts into a format to use with Processing. 
</description>

<method>
<mname>list()</mname>
<mdescription>Gets a list of the fonts installed on the system.</mdescription>
</method>

<constructor>
PFont()
PFont(<c>input</c>)
</constructor>

<cparameter>
<clabel>input</clabel>
<cdescription>InputStream</cdescription>
</cparameter>

<returns></returns>

<related>
loadFont() 
textFont() 
text() 
</related>

<availability>1.0</availability>

<type>Object</type>

<partof>Core</partof>








</root>
