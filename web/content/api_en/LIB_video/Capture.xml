<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>Capture</name>

<category>Video</category>

<subcategory></subcategory>

<usage>Application</usage>

<example>
<image></image>
<code>
import processing.video.*;
Capture myCapture;

void setup() 
{
  size(200, 200);

  // The name of the capture device is dependent those
  // plugged into the computer. To get a list of the 
  // choices, uncomment the following line 
  // println(Capture.list());
  // And to specify the camera, replace "Camera Name" 
  // in the next line with one from Capture.list()
  // myCapture = new Capture(this, width, height, "Camera Name", 30);
  
  // This code will try to use the last device used
  // by a QuickTime program
  myCapture = new Capture(this, width, height, 30);
}

void captureEvent(Capture myCapture) {
  myCapture.read();
}

void draw() {
  image(myCapture, 0, 0);
}

</code>
</example>

<description>
Datatype for storing and manipulating video frames from an attached capture device such as a camera. Use <b>Capture.list()</b> to show the names of any attached devices. Using the version of the constructor without <b>name</b> will attempt to use the last device used by a QuickTime program.
</description>

<syntax>
</syntax>

<parameters>
</parameters>

<method>
<mname>read()</mname>
<mdescription>Reads the current image</mdescription>
</method>

<method>
<mname>available()</mname>
<mdescription>Returns true when a new image is available</mdescription>
</method>

<method>
<mname>list()</mname>
<mdescription>Lists the devices current attached and on</mdescription>
</method>

<method>
<mname>frameRate()</mname>
<mdescription>Sets how often new frames are read</mdescription>
</method>

<method>
<mname>stop()</mname>
<mdescription>Stops capturing frames</mdescription>
</method>

<constructor>
Capture(<c>parent</c>, <c>width</c>, <c>height</c>)
Capture(<c>parent</c>, <c>width</c>, <c>height</c>, <c>fps</c>)
Capture(<c>parent</c>, <c>width</c>, <c>height</c>, <c>name</c>, )
Capture(<c>parent</c>, <c>width</c>, <c>height</c>, <c>name</c>, <c>fps</c>)
</constructor>

<cparameter>
<clabel>parent</clabel>
<cdescription>PApplet: typically use "this"</cdescription>
</cparameter>

<cparameter>
<clabel>width</clabel>
<cdescription>int: width of the frame</cdescription>
</cparameter>

<cparameter>
<clabel>height</clabel>
<cdescription>int: height of the frame</cdescription>
</cparameter>

<cparameter>
<clabel>fps</clabel>
<cdescription>int: number of frames to read per second</cdescription>
</cparameter>

<cparameter>
<clabel>name</clabel>
<cdescription>String: name of the camera</cdescription>
</cparameter>

<returns></returns>

<related>
captureEvent()
</related>

<availability>1.0</availability>

<type>Object</type>

<partof>Library</partof>

</root>
