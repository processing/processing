<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>noCrop()</name>

<category>Capture</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
import processing.video.*;
Capture myCapture;

void setup() 
{
  size(200, 200);
  print(Capture.list());
  String s = "Logitech QuickCam Zoom-WDM";
  myCapture = new Capture(this, s, width, height, 30);
  myCapture.crop(10, 10, 180, 180);
}

void draw() {
  if(myCapture.available()) {
    myCapture.read();
  }
  image(myCapture, 10, 10);
}

void mousePressed() {
  myCapture.noCrop();
}

</code>
</example>

<description>
Removed the cropping set by the <b>crop()</b> function.
</description>

<syntax>
<c>cam</c>.noCrop()
</syntax>

<parameter>
<label>cam</label>
<description>any variable of type Capture</description>
</parameter>
<parameter>

<returns></returns>

<related></related>

<availability>1.0</availability>

<type>Method</type>

<partof>Library</partof>


</root>
