<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>div()</name>

<category>PVector</category>

<subcategory>Method</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
PVector v;

void setup() {
  smooth();
  noLoop();
  v = new PVector(30, 60, 0);
}

void draw() {
  ellipse(v.x, v.y, 12, 12);
  v.div(6);
  ellipse(v.x, v.y, 24, 24);
}
</code>
</example>

<example>
<image></image>
<code>
PVector v1, v2;

void setup() {
  smooth();
  noLoop();
  v1 = new PVector(90, 80, 0);
  v2 = new PVector(2, 10, 0); 
}

void draw() {
  ellipse(v1.x, v1.y, 12, 12);
  ellipse(v2.x, v2.y, 12, 12);
  v1.div(v2);
  ellipse(v1.x, v1.y, 24, 24);
}
</code>
</example>

<example>
<image></image>
<code>
PVector v1;

void setup() {
  smooth();
  noLoop();
  v1 = new PVector(30, 60, 0);
}

void draw() {
  ellipse(v1.x, v1.y, 12, 12);
  PVector v2 = PVector.div(v1, 6);
  ellipse(v2.x, v2.y, 24, 24);
}
</code>
</example>

<example>
<image></image>
<code>
PVector v1, v2;

void setup() {
  smooth();
  noLoop();
  v1 = new PVector(5, 10, 0);
  v2 = new PVector(15, 8, 0); 
}

void draw() {
  ellipse(v1.x, v1.y, 12, 12);
  ellipse(v2.x, v2.y, 12, 12);
  PVector v3 = PVector.div(v1, v2);
  ellipse(v3.x, v3.y, 24, 24);
}
</code>
</example>

<description>
Divides a vector by a scalar or divides one vector by another.
</description>

<syntax>
<c>vec</c>.div(<c>scalar</c>)
<c>vec</c>.div(<c>v</c>)
PVector.div(<c>v</c>, <c>scalar</c>)
PVector.div(<c>v1</c>, <c>v2</c>)
</syntax>

<parameter>
<label>vec</label>
<description>any variable of type PVector</description>
</parameter>

<parameter>
<label>scalar</label>
<description>float: the number to multiply with the vector</description>
</parameter>

<parameter>
<label>v</label>
<description>PVector: the vector to multiply by the scalar</description>
</parameter>

<parameter>
<label>v1</label>
<description>PVector: the x, y, and z components of a PVector object</description>
</parameter>

<parameter>
<label>v2</label>
<description>PVector: the x, y, and z components of a PVector object</description>
</parameter>

<returns>None or PVector</returns>

<related></related>

<availability>1.0</availability>

<type>Method</type>

<partof>Core</partof>

<level>Extended</level>


</root>
