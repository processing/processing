<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>updatePixels()</name>

<category>Image</category>

<subcategory>Pixels</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>updatePixels_.jpg</image>
<code>
int halfImage = width*height/2;
PImage myImage = loadImage("topanga.jpg");
image(myImage, 0, 0);

loadPixels();
for(int i=0; i<halfImage; i++) {
  pixels[i+halfImage] = pixels[i];
}
updatePixels();
</code>
</example>

<example>
<image>updatePixels_2.jpg</image>
<code>
int halfImage = width*height/2;
PImage myImage = loadImage("towerFlip.jpg");
image(myImage, 0, 0);

loadPixels();
for(int i=0; i<halfImage; i++) {
  pixels[i+halfImage] = pixels[i];
}

updatePixels(50, 0, 50, 100);
</code>
</example>

<description>
Updates the display window with the current data from <b>pixels[]</b>. It's possible to only update a portion of the window by specifying a smaller rectangle with the <b>x1</b>, <b>y1</b>, <b>width</b>, <b>height</b> parameters. The <b>imageMode()</b> function changes the way the parameters work. For example, <b>imageMode(CENTER)</b> will change <b>x</b> and <b>y</b> to define the center of the image. A call to <b>imageMode(CORNERS)</b> will change the <b>width</b> and <b>height</b> parameters to define the x and y coordinates of the opposite corner of the image.
</description>

<syntax>
updatePixels()
updatePixels(<c>x1</c>, <c>y1</c>, <c>width</c>, <c>height</c>)
</syntax>

<parameter>
<label>x1</label>
<description>int: x-coordinate for the upper-left corner</description>
</parameter>

<parameter>
<label>y1</label>
<description>int: y-coordinate for the upper-right corner</description>
</parameter>

<parameter>
<label>x2</label>
<description>int: width or x-coordinate for the opposite corner depending on the current imageMode()</description>
</parameter>

<parameter>
<label>y2</label>
<description>int: height or y-cooordinate for the opposite corner depending on the current imageMode()</description>
</parameter>

<returns></returns>

<related>
pixels[]
loadPixels()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
