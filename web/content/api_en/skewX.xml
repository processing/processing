<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>skewX()</name>

<category>Transform</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>skewX_.gif</image>
<code>
size(100, 100, P2D);
translate(width/4, height/4);
skewX(PI/4.0);
rect(0, 0, 30, 30);
</code>
</example>

<description>
Skews a shape around the x-axis the amount specified by the <b>angle</b> parameter. Angles should be specified in radians (values from 0 to PI*2) or converted to radians with the <b>radians()</b> function. Objects are always skewed around their relative position to the origin and positive numbers skew objects in a clockwise direction. Transformations apply to everything that happens after and subsequent calls to the function accumulates the effect. For example, calling <b>skewX(PI/2)</b> and then <b>skewX(PI/2)</b> is the same as <b>skewX(PI)</b>. If <b>skewX()</b> is called within the <b>draw()</b>, the transformation is reset when the loop begins again. This function works in P2D or JAVA2D mode as shown in the example above.
<br/> <br/>
Technically, <b>skewX()</b> multiplies the current transformation matrix by a rotation matrix. This function can be further controlled by the <b>pushMatrix()</b> and <b>popMatrix()</b> functions.
</description>

<syntax>
skewX(<c>angle</c>); 
</syntax>

<parameter>
<label>angle</label>
<description>float: angle of skew specified in radians</description>
</parameter>

<returns>None</returns>

<related>
skewY() 
translate() 
scale() 
pushMatrix() 
popMatrix() 
radians()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
