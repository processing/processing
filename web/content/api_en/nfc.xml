<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>nfc()</name>

<category>Data</category>

<subcategory>String Functions</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
int i = 500000; 
String si = nfc(i);
println(si);  // Prints "500,000"
float f = 42525.34343; 
String fi = nfc(f, 2);
println(fi);  // Prints "42,525.34"

</code>
</example>

<description>
Utility function for formatting numbers into strings and placing appropriate commas to mark units of 100. There are two versions, one for formatting ints and one for formatting an array of ints. The value for the <b>digits</b> parameter should always be a positive integer.
</description>

<syntax>
nfc(<c>intValue</c>)
nfc(<c>floatValue</c>, <c>right</c>)
</syntax>

<parameter>
<label>intValue</label>
<description>int or int[]: the number(s) to format</description>
</parameter>

<parameter>
<label>floatValue</label>
<description>float or float[]: the number(s) to format</description>
</parameter>

<parameter>
<label>right</label>
<description>int: number of digits to the right of the decimal point</description>
</parameter>

<returns>String or String[]</returns>

<related>
nf()
nfs()
nfp()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>


<level>Extended</level>





</root>
