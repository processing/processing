<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>endShape()</name>

<category>Shape</category>

<subcategory>Vertex</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>endShape_.gif</image>
<code>
noFill();

beginShape();
vertex(20, 20);
vertex(45, 20);
vertex(45, 80);
endShape();

beginShape();
vertex(50, 20);
vertex(75, 20);
vertex(75, 80);
endShape(CLOSE);

</code>
</example>

<description>
The <b>endShape()</b> function is the companion to <b>beginShape()</b> and may only be called after the later. When <b>endshape()</b> is called, all of image data defined since the previous call to <b>beginShape()</b> is written into the image buffer. The the constant CLOSE as the value for the MODE parameter to close the shape (to connect the beginning and the end). 
</description>

<syntax>
beginShape()
beginShape(<c>MODE</c>)
</syntax>

<parameter>
<label>MODE</label>
<description>Use CLOSE to close the shape</description>
</parameter>

<returns>None</returns>

<related>
beginShape()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
