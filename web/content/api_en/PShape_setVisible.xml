<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>setVisible()</name>

<category>PShape</category>

<subcategory>Method</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>PShape_visible.jpg</image>
<code>
// The file "bot.svg" must be in the data folder
// of the current sketch to load successfully
PShape s;
s = loadShape("bot.svg");
smooth();
shape(s, 10, 10, 80, 80);      // Right shape
s.setVisible(false);
if (s.isVisible() == false) {  // of if (!s.isVisible) {
  noFill();
  rect(5, 5, 90, 90); 
}
</code>
</example>

<description>
Sets the shape to be visible or invisible. This is determined by the value of the <b>visible</b> parameter.
<br/> <br/>
The visibility of a shape is usually controlled by whatever program created the SVG file. For instance, this parameter is controlled by showing or hiding the shape in the layers palette in Adobe Illustrator.
</description>

<syntax>
<c>sh</c>.setVisible(<c>visible</c>)
</syntax>

<parameter>
<label>sh</label>
<description>PShape: any variable of type PShape</description>
</parameter>

<parameter>
<label>visible</label>
<description>Boolean: "false" makes the shape invisible and "true" makes it visible</description>
</parameter>

<returns>none</returns>

<related>
</related>

<availability>1.0</availability>

<type>Method</type>

<partof>Core</partof>


</root>
