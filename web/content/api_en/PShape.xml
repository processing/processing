<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>PShape</name>

<category>Shape</category>

<subcategory>PShape</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>svg.jpg</image>
<code>
PShape s;
s = loadShape("bot.svg");
smooth();
shape(s, 10, 10, 80, 80);
</code>
</example>

<description>
Datatype for storing shapes. Processing can currently load and display SVG (Scalable Vector Graphics) shapes. Before a shape is used, it must be loaded with the <b>loadShape()</b> function. The <b>shape()</b> function is used to draw the shape to the display window. The <b>PShape</b> object contain a group of methods, linked below, that can operate on the shape data. 
<br /><br />
This library was specifically tested using SVG files created from Adobe Illustrator. For now, we can't guarantee that it will work for SVGs created with anything else.
</description>

<syntax></syntax>

<parameters></parameters>

<method>
<mname>isVisible()</mname>
<mdescription>Returns a boolean value "true" if the image is set to be visible, "false" if not</mdescription>
</method>

<method>
<mname>setVisible()</mname>
<mdescription>Sets the shape to be visible or invisible</mdescription>
</method>

<method>
<mname>disableStyle()</mname>
<mdescription>Disables the shape's style data and uses Processing styles</mdescription>
</method>

<method>
<mname>enableStyle()</mname>
<mdescription>Enables the shape's style data and ignores the Processing styles</mdescription>
</method>

<method>
<mname>getChild()</mname>
<mdescription>Returns a child element of a shape as a PShape object</mdescription>
</method>

<method>
<mname>translate()</mname>
<mdescription>Displaces the shape</mdescription>
</method>

<method>
<mname>rotate()</mname>
<mdescription>Rotates the shape</mdescription>
</method>

<method>
<mname>rotateX()</mname>
<mdescription>Rotates the shape around the x-axis</mdescription>
</method>

<method>
<mname>rotateY()</mname>
<mdescription>Rotates the shape around the y-axis</mdescription>
</method>

<method>
<mname>rotateZ()</mname>
<mdescription>Rotates the shape around the z-axis</mdescription>
</method>

<method>
<mname>scale()</mname>
<mdescription>Increases and decreases the size of a shape</mdescription>
</method>

<constructor>
PShape()
</constructor>


<returns></returns>

<related>
shape()
loadShape()
shapeMode()
</related>

<availability>1.0</availability>

<type>Object</type>

<partof>Core</partof>


</root>
