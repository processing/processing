<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>rotateX()</name>

<category>PShape</category>

<subcategory>Method</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>PShape_rotateX.gif</image>
<code>
size(100, 100, P3D);
// The file "ohio.svg" must be in the data folder
// of the current sketch to load successfully
PShape s;
s = loadShape("ohio.svg");
s.rotateX(PI/6);
shape(s, 10, 0);
</code>
</example>

<description>
Rotates a shape around the x-axis the amount specified by the <b>angle</b> parameter. Angles should be specified in radians (values from 0 to TWO_PI) or converted to radians with the <b>radians()</b> function.
<br /><br />
Shapes are always rotated around the upper-left corner of their bounding box. Positive numbers rotate objects in a clockwise direction. Transformations apply to everything that happens after and subsequent calls to the function accumulates the effect. For example, calling <b>rotate(HALF_PI)</b> and then <b>rotate(HALF_PI)</b> is the same as <b>rotate(PI)</b>. All tranformations are reset when <b>draw()</b> begins again.
<br /><br />
This function requires a 3D renderer. You need to pass P3D or OPENGL as a third parameter into the <b>size()</b> function as shown in the example above.
</description>

<syntax>
<c>sh</c>.rotateX(<c>angle</c>)
</syntax>

<parameter>
<label>sh</label>
<description>PShape: any variable of type PShape</description>
</parameter>

<parameter>
<label>angle</label>
<description>float: angle of rotation specified in radians</description>
</parameter>

<returns>none</returns>

<related>
</related>

<availability>1.0</availability>

<type>Method</type>

<partof>Core</partof>


</root>
