<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>blendColor()</name>

<category>Color</category>

<subcategory>Creating &amp; Reading</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>blend_color.gif</image>
<code>
color orange = color(204, 102, 0);
color blue = color(0, 102, 153);
color orangeblueadd = blendColor(orange, blue, ADD);
background(51);
noStroke();
fill(orange);
rect(14, 20, 20, 60);
fill(orangeblueadd);
rect(40, 20, 20, 60);
fill(blue);
rect(66, 20, 20, 60);
</code>
</example>

<description>
Blends two color values together based on the blending mode given as the <b>MODE</b> parameter. There is a choice of the following modes to blend the source pixels (A) with the ones of pixels in the destination image (B):<br /> 
<br />
BLEND - linear interpolation of colours: C = A*factor + B <br />
ADD - additive blending with white clip: C = min(A*factor + B, 255)<br /> 
SUBTRACT - subtractive blending with black clip: C = max(B - A*factor, 0)<br /> 
DARKEST - only the darkest colour succeeds: C = min(A*factor, B)<br />
LIGHTEST - only the lightest colour succeeds: C = max(A*factor, B)
</description>

<syntax>
blendColor(<c>c1</c>, <c>c2</c>, <c>MODE</c>)
</syntax>

<parameter>
<label>c1</label>
<description>color: the first color to blend</description>
</parameter>

<parameter>
<label>c2</label>
<description>color: the second color to blend</description>
</parameter>

<parameter>
<label>MODE</label>
<description>Either BLEND, ADD, SUBTRACT, LIGHTEST, DARKEST</description>
</parameter>

<returns>float</returns>

<related>
blend()
color()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>


</root>
