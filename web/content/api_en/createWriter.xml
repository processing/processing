<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>createWriter()</name>

<category>Output</category>

<subcategory>Files</subcategory>

<usage>Application</usage>

<example>
<image></image>
<code>
PrintWriter output;

void setup() {
  // Create a new file in the sketch directory
  output = createWriter("positions.txt"); 
}

void draw() {
  point(mouseX, mouseY);
  output.println(mouseX); // Write the coordinate to the file
}

void keyPressed() {
  output.flush(); // Writes the remaining data to the file
  output.close(); // Finishes the file
  exit(); // Stops the program
}

</code>
</example>

<description>
Creates a new file in the sketch folder, and a <b>PrintWriter</b> object to write to it. For the file to be made correctly, it should be flushed and must be closed with its <b>flush()</b> and <b>close()</b> methods (see above example). 
</description>

<syntax>
createWriter(<c>filename</c>)
</syntax>

<parameter>
<label>filename</label>
<description>Name of the file to be created</description>
</parameter>

<returns>PrintWriter or null</returns>

<related>
PrintWriter
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>

</root>
