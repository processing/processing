<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>mousePressed</name>

<category>Input/Output</category>

<subcategory>Mouse</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>mousePressed.jar</image>
<code>
// Click within the image to change 
// the value of the rectangle
void loop() {
  if (mousePressed == true) {
    fill(0);
  } else {
    fill(255);
  }
  rect(25, 25, 50, 50);
}
</code>
</example>

<description>
Processing automatically tracks if the mouse button is pressed or not (it tracks the left mouse button for mice with more than one button). The value of the system variable <b>mousePressed</b> is true is the button is pressed and false if the button is not pressed.
</description>

<syntax>
</syntax>

<parameter>
<label></label>
<description></description>
</parameter>

<returns></returns>

<related>
mouseX
mouseY
pmouseX
pmouseY
mousePressed()
mouseReleased()
mouseMoved()
mouseDragged()
</related>

<availability>1.0</availability>

<type>System variable</type>

<partof>Core</partof>








</root>
