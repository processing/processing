<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>asin()</name>

<category>Math</category>

<subcategory>Trigonometry</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
float a = PI/3;
float s = sin(a);
float as = asin(s);
// Prints "1.0471976 : 0.86602545 : 1.0471976"
println(a + " : " + s + " : " +  as);
</code>
</example>

<example>
<image></image>
<code>
float a = PI + PI/3.0;
float s = sin(a);
float as = asin(s);
// Prints "4.1887903 : -0.86602545 : -1.0471976"
println(a + " : " + s + " : " +  as);
</code>
</example>

<example>
<image></image>
<code>
angleMode(DEGREES);
float a = 30;
float s = sin(a);
float as = asin(s);
// Prints "30.0 : 0.5 : 30.0"
println(a + " : " + s + " : " +  as);
</code>
</example>

<description>
The inverse of <b>sin()</b>, returns the arc sine of a value. This function expects the values in the range of -1 to 1 and values are returned in the range <b>0</b> to <b>PI (3.1415927)</b> or from <b>0</b> to <b>180</b> depending on the current angle mode.
</description>

<syntax>
asin(<c>value</c>)
</syntax>

<parameter>
<label>value</label>
<description>float: numbers between -1 and 1<description>
</parameter>

<returns>float</returns>

<related>
sin()
acos()
atan()
angleMode()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>

<level>Extended</level>


</root>
