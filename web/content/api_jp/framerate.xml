<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>framerate()</name>

<category>Environment</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>framerate_.jar</image>
<code>
void setup() {
  framerate(4);
}
int pos = 0;
void loop() {
  background(204);
  pos++;
  line(pos, 20, pos, 80);
  if(pos &gt; width) {
    pos = 0;
  }
}
</code>
</example>

<description>
Specifies the number of frames to be displayed every second. If the processor is not fast enough to maintain this rate, the frames will not display at the specified rate. The function call <b>framerate(30)</b> will attempt to refresh 30 times a second.
</description>

<syntax>
framerate(<c>fps</c>)
</syntax>

<parameter>
<label>fps</label>
<description>int: number of frames per second</description>
</parameter>

<returns>None</returns>

<related>
delay()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
