<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>get()</name>

<category>Image</category>

<subcategory>Pixels</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>get_.jar</image>
<code>
void loop() {
  for(int i=0; i&lt;height; i++) {
    stroke(i*2);
    line(0, i, 50, i);
  }
  color c = get(mouseX, mouseY);
  noStroke();
  fill(c);
  rect(50, 0, 50, 100);
}
</code>
</example>

<description>
Reads the color of any pixel or grabs a group of pixels. The numbers returned are scaled according to the current color ranges, but only RGB values are returned by this function. Even though you may have drawn a shape with <b>colorMode(HSB)</b>, the numbers returned will be in RGB. If the pixel requested is outside of the image window, the background color is returned. Get a section of the display window by specifying a <b>width</b> and <b>height</b> parameter. The version without parameters returns the entire display window.
</description>

<syntax>
get()
get(<c>x</c>, <c>y</c>)
get(<c>x</c>, <c>y</c>, <c>width</c>, <c>height</c>)
</syntax>

<parameter>
<label>x</label>
<description>int: x-coordinate of the pixel</description>
</parameter>

<parameter>
<label>y</label>
<description>int: y-coordinate of the pixel</description>
</parameter>

<parameter>
<label>width</label>
<description>int: width of pixel rectangle to get</description>
</parameter>

<parameter>
<label>height</label>
<description>int: height of pixel rectangle to get</description>
</parameter>

<returns>color or PImage</returns>

<related>
set()
pixels[]
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
