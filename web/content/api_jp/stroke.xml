<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>stroke()</name>

<category>Color</category>

<subcategory>Setting</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>stroke_.gif</image>
<code>
stroke(153);
rect(30, 20, 55, 55);
</code>
</example>

<example>
<image>stroke_2.gif</image>
<code>
stroke(204, 102, 0);
rect(30, 20, 55, 55);
</code>
</example>

<description>
Sets the stroke color which is the color used to draw lines and borders around shapes. This color is either specified in terms of the RGB or HSB color depending on the current <b>colorMode()</b>. The default color space is RGB, with each value in the range from 0 to 255. The version of <b>stroke()</b> with one parameter specifies a gray value, the version with three parameters specifies a color, and the version with four parameters specifies a color with an alpha value. <i>Note: the value for the parameter "gray" must be less than or equal to the current maximum value as specified by <b>colorMode()</b>. The default value is 255.</i>
</description>

<syntax>
stroke(<c>gray</c>)
stroke(<c>value1</c>, <c>value2</c>, <c>value3</c>);
stroke(<c>color</c>);
</syntax>

<parameter>
<label>gray</label>
<description>int or float: specifies a value between white and black</description>
</parameter>

<parameter>
<label>value1</label>
<description>int or float: red or hue value (depending on the current color mode)</description>
</parameter>

<parameter>
<label>value2</label>
<description>int or float: green or saturation value (depending on the current color mode)</description>
</parameter>

<parameter>
<label>value3</label>
<description>int or float: blue or brightness value (depending on the current color mode)</description>
</parameter>

<parameter>
<label>color</label>
<description>color: any value of the color datatype</description>
</parameter>

<returns>None</returns>

<related>
noStroke()
colorMode()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
