<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>scale()</name>

<category>Transform</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>scale_.gif</image>
<code>
rect(30, 20, 50, 50);
scale(0.5);
rect(30, 20, 50, 50);
</code>
</example>

<example>
<image>scale_2.gif</image>
<code>
rect(30, 20, 50, 50);
scale(0.5, 1.3);
rect(30, 20, 50, 50);
</code>
</example>

<description>
Increasing and decreasing the size of an object by expanding and contracting vertices. Scale values are specified as decimal percentages. The function call <b>scale(2.0)</b> increases the dimension of a shape by 200%. Objects always scale from their relative origin to the coordinate system. Transformations apply to everything that happens after and subsequent calls to the function multiply the effect. For example, calling <b>scale(2.0)</b> and then <b>scale(1.5)</b> is the same as <b>scale(2.5)</b>. If <b>scale()</b> is called within the <b>loop()</b>, the transformation is reset when the loop begins again. 
</description>

<syntax>
scale(<c>size</c>);
scale(<c>x</c>, <c>y</c>);
scale(<c>x</c>, <c>y</c>, <c>z</c>);
</syntax>

<parameter>
<label>size</label>
<description>float: percentage to scale the object</description>
</parameter>

<parameter>
<label>x</label>
<description>float: percentage to scale the object in the "x" axis</description>
</parameter>

<parameter>
<label>y</label>
<description>float: percentage to scale the object in the "y" axis</description>
</parameter>

<parameter>
<label>z</label>
<description>float: percentage to scale the object in the "z" axis</description>
</parameter>

<returns>None</returns>

<related>
translate() 
rotate() 
rotateX() 
rotateY() 
rotateZ() 
scale() 
push() 
pop() 
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
