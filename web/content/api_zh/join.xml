<?xml version="1.0" encoding="big5"?>
<root>
<name>join() 結合</name>

<category>Data</category>

<subcategory>Parsing</subcategory>

<usage>網路及應用程式</usage>

<example>
<image></image>
<code>
// works identically with float[] and String[]
int list[] = new list[3];
list[0] = 8;
list[1] = 67;
list[2] = 5;
String formatted = join(list, ", ");
// formatted now contains "8, 67, 5"

// to format the number of digits used
// the '3' means to pad with zeroes up to 3 digits
String withzeroes = join(list, " ", 3);
// withzeros now contains "008 067 005"

// for floats, formatting is more complicated because 
// there are also digits after the decimal point.
float f[] = new float[3];
f[0] = 1.3;
f[1] = 92.8;
f[2] = 0.7;
// 3 digits on the left of the decimal point
// 2 digits to the right of the decimal point
String zerofloats = join(f, " ", 3, 2);
// zerofloats now contains "001.30 092.80 007.70"

// or if you don't want to pad the left-hand side
// a zero will say to ignore and don't pad
String lesspadding = join(f, " ", 0, 2);
// lesspadding now contains "1.30 92.80 7.70"

</code>
</example>

<description>
結合陣列中的元素至某字串中。
</description>

<syntax>
join(<c>anyArray</c>, <c>separator</c>)
join(<c>intArray</c>, <c>separator</c>, <c>digits</c>)
join(<c>floatArray</c>, <c>separator</c>, <c>left</c>, <c>right</c>)
</syntax>

<parameter>
<label>array</label>
<description>字串,整數或浮點數之陣列</description>
</parameter>

<parameter>
<label>intArray</label>
<description>整數之陣列
</description>
</parameter>

<parameter>
<label>floatArray</label>
<description>浮點數之陣列</description>
</parameter>

<parameter>
<label>separator</label>
<description>字串:隔開項目之間的字串</description>
</parameter>

<parameter>
<label>digits</label>
<description>整數:在數字前加上0後，其變成幾位數。</description>
</parameter>

<parameter>
<label>left</label>
<description>整數:小數點左邊的數字數目</description>
</parameter>

<parameter>
<label>right</label>
<description>整數:小數點右邊的數字數目</description>
</parameter>

<returns>字串</returns>

<related>
splitInts()
splitFloats()
splitStrings()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Bagel</partof>








</root>
