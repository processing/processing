<?xml version="1.0" encoding="big5"?>
<root>
<name>sqrt() 平方根</name>

<category>Math</category>

<subcategory>Basic Functions</subcategory>

<usage>網路及應用程式</usage>

<example>
<image>sqrt_.gif</image>
<code>
noStroke();
float a = sqrt(6561);  // Sets a to 81
float b = sqrt(625);   // Sets b to 25
float c = sqrt(1);     // Sets c to 1
rect(0, 25, a, 10);
rect(0, 45, b, 10);
rect(0, 65, c, 10);
</code>
</example>

<description>
計算一個數值的平方根。平方根必定是正數，即使有效的平方根亦可為負數。數值 <b>a</b> 的平方根 <b>s</b> 是指<b>s*s = a</b>。平方根亦即平方的相反。
</description>

<syntax>
sqrt(<c>value</c>)
</syntax>

<parameter>
<label>value</label>
<description>整數或浮點數：非負數的數值</description>
</parameter>

<returns>浮點數</returns>

<related>
pow()
sq()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>PDE</partof>








</root>
