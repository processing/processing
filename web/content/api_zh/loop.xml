<?xml version="1.0" encoding="big5"?>
<root>
<name>loop() 迴圈</name>

<category>Structure</category>

<subcategory></subcategory>

<usage>網路及應用程式</usage>

<example>
<image></image>
<applet></applet>
<code>
void setup() {
  size(200, 200);
  noStroke();
}

int a = 0;

void loop() {
  fill(a);
  a = a + 1;
  if (a &gt; width) {
    a = 0;
  }
  rect(a, 0, 2, 200); 
}
</code>
</example>

<description>
連續不斷地執行迴圈組成部份內的程式，直到程式終止。 <b>loop()</b>函式和<b>setup()</b>函式搭配使用。每秒內 <b>loop()</b>執行的次數可以<b>delay()</b>和 <b>framerate()</b> 函式控制。
</description>

<syntax>
loop() {
  <c>statements</c>
}
</syntax>

<parameter>
<label>statements</label>
<description>一系列反覆執行的宣告</description>
</parameter>

<returns>無</returns>

<related>
setup()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>PDE</partof>








</root>
