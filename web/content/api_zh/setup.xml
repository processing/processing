<?xml version="1.0" encoding="big5"?>
<root>
<name>setup() 宣告初始值</name>

<category>Structure</category>

<subcategory></subcategory>

<usage>網路及應用程式</usage>

<example>
<image></image>
<code>
void setup() {
  size(200, 200);
  background(0);
  noStroke();
  fill(102);
}

int a = 0;

void loop() {
  rect(a++%width, 10, 2, 80); 
}
</code>
</example>

<description>
程式啟動時被召喚一次的函式。用於 <b>loop()</b> 函式執行前，以定義初始的環境特質，如視窗大小、背景顏色、圖像載入等。<b>setup()</b> 函式不適用於<b>loop()</b> 函式之中。
</description>

<syntax>
void setup() {
  <c>statements</c>
}
</syntax>

<parameter>
<label>statements</label>
<description>任何有效的陳述</description>
</parameter>

<returns>無</returns>

<related>
loop()
size()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>PDE</partof>








</root>
