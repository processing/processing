<?xml version="1.0" encoding="big5"?>
<root>
<name>if() 條件式</name>

<category>Control</category>

<subcategory>Conditionals</subcategory>

<usage>網路及應用程式</usage>

<example>
<image>if_.gif</image>
<code>
for(int i=5; i&lt;height; i+=5) {
  stroke(255);  //Set the color to white
  if(i &lt; 35) {  //When "i" is less than "35"...
    stroke(0);  //...set the color to black
  }
  line(30, i, 80, i);
}
</code>
</example>

<description>
決定該執行哪一部份的程式碼。如果表述（<b>expression</b>）為成立（<b>true</b>），其後組成部份內的宣告就會被執行。如果表述（<b>expression</b>）為不成立（ <b>false</b>），其後組成部份內的宣告便不會被執行。
</description>

<syntax>
if(<c>expression</c>) { 
  <c>statements</c> 
} 
</syntax>

<parameter>
<label>expression</label>
<description>任何成立或不成立的表述</description>
</parameter>

<parameter>
<label>statements</label>
<description>等待被執行的1個或多個宣告</description>
</parameter>

<returns></returns>

<related>
else
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>PDE</partof>








</root>
