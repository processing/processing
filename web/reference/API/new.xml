<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>new</name>

<category>Structure</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code>
HLine h1 = new HLine();
int[] speeds = new int[3];
int ypos;

void setup() { 
  size(200, 200);
  speeds[0] = 1; 
  speeds[1] = 2;
  speeds[2] = 5;
} 
 
void draw() {
  ypos += speeds[random(3)]; 
  if (ypos &gt; width) { 
    ypos = 0; 
  } 
  h1.update(ypos); 
} 
 
class HLine { 
  void update(int y) { 
    line(0, y, width, y); 
  } 
} 

</code>
</example>

<description>
Creates a "new" object. The keyword <b>new</b> is typically used similarly to the applications in the above example. In this example, a new object "h1" of the datatype "HLine" is created. On the following line, a new array of ints called "speeds" is created.
</description>

<syntax>
new 
</syntax>

<parameter>
<label></label>
</parameter>

<returns></returns>

<related></related>

<availability>1.0</availability>

<type>keyword</type>

<partof>PDE</partof>



</root>
