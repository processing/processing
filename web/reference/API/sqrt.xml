<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>sqrt()</name>

<category>Math</category>

<subcategory>Fixed Point - Calculation</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>sqrt_.gif</image>
<code>
noStroke(); 
int a_fp = sqrt(itofp(6561));  // Sets a_fp to 81.0 
int b_fp = sqrt(itofp(625));   // Sets b_fp to 25.0 
int c_fp = sqrt(itofp(1));     // Sets c_fp to 1.0 
rect(0, 25, fptoi(a_fp), 10); 
rect(0, 45, fptoi(b_fp), 10); 
rect(0, 65, fptoi(c_fp), 10); 
</code>
</example>

<description>
Calculates the square root of a fixed point value and returns a fixed point value. The square root of a number is always positive, even though there may be a valid negative root. The square root <b>s</b> of number <b>a</b> is such that <b>s*s = a</b>. It is the opposite of squaring.
</description>

<syntax>
sqrt(<c>value</c>)
</syntax>

<parameter>
<label>value</label>
<description>int fixed point value: non-negative number</description>
</parameter>

<returns>int fixed point value</returns>

<related>
pow()
sq()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>PDE</partof>








</root>
