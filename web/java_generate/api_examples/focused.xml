<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>focused</name>

<category>Environment</category>

<subcategory></subcategory>

<usage>Web</usage>

<example>
<image></image>
<code><![CDATA[
if (focused) {  // or "if (focused == true)"
  ellipse(25, 25, 50, 50);
} else {
  line(0, 0, 100, 100);
  line(100, 0, 0, 100);
}
]]></code>
</example>

<description><![CDATA[Confirms if a Processing program is "focused", meaning that it is active and will accept input from mouse or keyboard. This variable is "true" if it is focused and "false" if not. This variable is often used when you want to warn people they need to click on or roll over an applet before it will work.]]></description>

<syntax></syntax>

<returns></returns>

<related></related>

<availability>1.0</availability>

<type>System variable</type>

<partof>Core</partof>

<level>Extended</level>

</root>
