<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>|| (logical OR)</name>

<category>Control</category>

<subcategory>Logical Operators</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>logicalOR.gif</image>
<code><![CDATA[
for(int i=5 ; i&lt;=95; i+=5) {
  if((i &lt; 35) || (i &gt; 60)) {
    line(30, i, 80, i);
  }
}
]]></code>
</example>

<description><![CDATA[Compares two expressions and returns <b>true</b> if one or both evaluate to <b>true</b>. Returns <b>false</b> only if both expressions are <b>false</b>. The following list shows all possible combinations:<br /><br /><tt>true || false   // Evaluates true because the first is true <br />false || true   // Evaluates true because the second is true <br />true || true    // Evaluates true because both are true <br />false || false  // Evaluates false because both are false</tt> ]]></description>

<syntax><![CDATA[
<kbd>expression1</kbd> || <kbd>expression2</kbd>
]]></syntax>

<parameter>
<label>expression1</label>
<description><![CDATA[any valid expression]]></description>
</parameter>

<parameter>
<label>expression2</label>
<description><![CDATA[any valid expression]]></description>
</parameter>

<returns></returns>

<related>
&amp;&amp; (logical AND)
! (logical NOT)
if()
</related>

<availability>1.0</availability>

<type>Operator</type>

<partof>PDE</partof>








</root>
