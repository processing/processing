<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>new</name>

<category>Structure</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code><![CDATA[
HLine h1 = new HLine();
float[] speeds = new float[3];
float ypos;

void setup() { 
  size(200, 200);
  speeds[0] = 0.1; 
  speeds[1] = 2.0;
  speeds[2] = 0.5;
} 
 
void draw() {
  ypos += speeds[int(random(3))]; 
  if (ypos &gt; width) { 
    ypos = 0; 
  } 
  h1.update(ypos); 
} 
 
class HLine { 
  void update(float y) { 
    line(0, y, width, y); 
  } 
} 

]]></code>
</example>

<description><![CDATA[Creates a "new" object. The keyword <b>new</b> is typically used similarly to the applications in the above example. In this example, a new object "h1" of the datatype "HLine" is created. On the following line, a new array of floats called "speeds" is created.]]></description>

<syntax></syntax>

<parameter>
<label></label>
</parameter>

<returns></returns>

<related></related>

<availability>1.0</availability>

<type>keyword</type>

<partof>PDE</partof>



</root>
