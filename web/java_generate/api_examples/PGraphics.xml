<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>PGraphics</name>

<category>Rendering</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>pgraphics.jpg</image>
<code><![CDATA[
PGraphics pg;

void setup() {
  size(100, 100);
  pg = createGraphics(80, 80, P3D);
}

void draw() {
  pg.beginDraw();
  pg.background(102);
  pg.stroke(255);
  pg.line(40, 40, mouseX, mouseY);
  pg.endDraw();
  image(pg, 10, 10); 
}
]]></code>
</example>

<description><![CDATA[Main graphics and rendering context, as well as the base API implementation for processing "core". Use this class if you need to draw into an off-screen graphics buffer. A PGraphics object can be constructed with the <b>createGraphics()</b> function. The <b>beginDraw()</b> and <b>endDraw()</b> methods (see above example) are necessary to set up the buffer and to finalize it. The fields and methods for this class are extensive; for a complete list visit the developer's reference: http://dev.processing.org/reference/core/]]></description>

<method>
<mname>beginDraw()</mname>
<mdescription>Sets up the rendering context</mdescription>
</method>
<method>
<mname>endDraw()</mname>
<mdescription>Finalizes the renderering context</mdescription>
</method>

<syntax><![CDATA[
]]></syntax>

<parameters>
</parameters>

<constructor>
PGraphics()
PGraphics(<kbd>width</kbd>, <kbd>height</kbd>)
PGraphics(<kbd>width</kbd>, <kbd>height</kbd>, <kbd>applet</kbd>)
</constructor>

<cparameter>
<clabel>width</clabel>
<cdescription>int: width in pixels</cdescription>
</cparameter>

<cparameter>
<clabel>height</clabel>
<cdescription>int: height in pixels</cdescription>
</cparameter>

<cparameter>
<clabel>applet</clabel>
<cdescription>PApplet</cdescription>
</cparameter>


<returns></returns>

<related>
createGraphics()
</related>

<availability>1.0</availability>

<type>Object</type>

<partof>Core</partof>

<level>Extended</level>

</root>
