<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>popMatrix()</name>

<category>Transform</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>pop_.gif</image>
<code><![CDATA[
rect(0, 0, 50, 50);   //White rectangle
pushMatrix();
translate(30, 20);
fill(0);  
rect(0, 0, 50, 50);   //Black rectangle
popMatrix();
fill(102);  
rect(15, 10, 50, 50); //Gray rectangle
]]></code>
</example>

<description><![CDATA[Pops the current transformation matrix off the matrix stack. Understanding pushing and popping requires understanding the concept of a matrix stack. The <b>pushMatrix()</b> function saves the current coordinate system to the stack and <b>popMatrix()</b> restores the prior coordinate system. <b>pushMatrix()</b> and <b>popMatrix()</b> are used in conjuction with the other transformation methods and may be embedded to control the scope of the transformations.]]></description>

<syntax><![CDATA[
popMatrix()
]]></syntax>

<parameter>
<label></label>
<description></description>
</parameter>

<returns>None</returns>

<related>
pushMatrix()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
