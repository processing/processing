<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>div()</name>

<category>PVector</category>

<subcategory>Method</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code><![CDATA[
PVector v;

void setup() {
  smooth();
  noLoop();
  v = new PVector(30, 60, 0);
}

void draw() {
  ellipse(v.x, v.y, 12, 12);
  v.div(6);
  ellipse(v.x, v.y, 24, 24);
}
]]></code>
</example>

<example>
<image></image>
<code><![CDATA[
PVector v1, v2;

void setup() {
  smooth();
  noLoop();
  v1 = new PVector(90, 80, 0);
  v2 = new PVector(2, 10, 0); 
}

void draw() {
  ellipse(v1.x, v1.y, 12, 12);
  ellipse(v2.x, v2.y, 12, 12);
  v1.div(v2);
  ellipse(v1.x, v1.y, 24, 24);
}
]]></code>
</example>

<example>
<image></image>
<code><![CDATA[
PVector v1;

void setup() {
  smooth();
  noLoop();
  v1 = new PVector(30, 60, 0);
}

void draw() {
  ellipse(v1.x, v1.y, 12, 12);
  PVector v2 = PVector.div(v1, 6);
  ellipse(v2.x, v2.y, 24, 24);
}
]]></code>
</example>

<example>
<image></image>
<code><![CDATA[
PVector v1, v2;

void setup() {
  smooth();
  noLoop();
  v1 = new PVector(5, 10, 0);
  v2 = new PVector(15, 8, 0); 
}

void draw() {
  ellipse(v1.x, v1.y, 12, 12);
  ellipse(v2.x, v2.y, 12, 12);
  PVector v3 = PVector.div(v1, v2);
  ellipse(v3.x, v3.y, 24, 24);
}
]]></code>
</example>

<description><![CDATA[Divides a vector by a scalar or divides one vector by another.]]></description>

<syntax><![CDATA[
<kbd>vec</kbd>.div(<kbd>scalar</kbd>)
<kbd>vec</kbd>.div(<kbd>v</kbd>)
PVector.div(<kbd>v</kbd>, <kbd>scalar</kbd>)
PVector.div(<kbd>v1</kbd>, <kbd>v2</kbd>)
]]></syntax>

<parameter>
<label>vec</label>
<description><![CDATA[any variable of type PVector]]></description>
</parameter>

<parameter>
<label>scalar</label>
<description><![CDATA[float: the number to divide with the vector]]></description>
</parameter>

<parameter>
<label>v</label>
<description><![CDATA[PVector: the vector to divide by the scalar]]></description>
</parameter>

<parameter>
<label>v1</label>
<description><![CDATA[PVector: the x, y, and z components of a PVector object]]></description>
</parameter>

<parameter>
<label>v2</label>
<description><![CDATA[PVector: the x, y, and z components of a PVector object]]></description>
</parameter>

<returns>None or PVector</returns>

<related></related>

<availability>1.0</availability>

<type>Method</type>

<partof>Core</partof>

<level>Extended</level>


</root>
