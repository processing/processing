<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>rotateY()</name>

<category>Transform</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>rotateY_.gif</image>
<code><![CDATA[
size(100, 100, P3D);
translate(width/2, height/2);
rotateY(PI/3.0);
rect(-26, -26, 52, 52);
]]></code>
</example>

<example>
<image>rotateY_.gif</image>
<code><![CDATA[
size(100, 100, P3D);
translate(width/2, height/2);
rotateY(radians(60));
rect(-26, -26, 52, 52);
]]></code>
</example>

<description><![CDATA[Rotates a shape around the y-axis the amount specified by the <b>angle</b> parameter. Angles should be specified in radians (values from 0 to PI*2) or converted to radians with the <b>radians()</b> function. Objects are always rotated around their relative position to the origin and positive numbers rotate objects in a counterclockwise direction. Transformations apply to everything that happens after and subsequent calls to the function accumulates the effect. For example, calling <b>rotateY(PI/2)</b> and then <b>rotateY(PI/2)</b> is the same as <b>rotateY(PI)</b>. If <b>rotateY()</b> is called within the <b>draw()</b>, the transformation is reset when the loop begins again. This function requires passing P3D or OPENGL into the size() parameter as shown in the example above. ]]></description>

<syntax><![CDATA[
rotateY(<kbd>angle</kbd>)
]]></syntax>

<parameter>
<label>angle</label>
<description><![CDATA[float: angle of rotation specified in radians]]></description>
</parameter>

<returns>None</returns>

<related>
rotateX() 
rotateZ() 
translate() 
rotate() 
scale() 
pushMatrix() 
popMatrix() 
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>

<level>Extended</level>

</root>
