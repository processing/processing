<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>blue()</name>

<category>Color</category>

<subcategory>Creating &amp; Reading</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>blue_.gif</image>
<code><![CDATA[
noStroke();
color c = color(0, 126, 255);
fill(c);
rect(15, 20, 35, 60);
float value = blue(c);  // Sets "value" to "255"
fill(value);
rect(50, 20, 35, 60);
]]></code>
</example>

<description><![CDATA[Extracts the blue value from a color, scaled to match current <b>colorMode()</b>. This value is always returned as a  float so be careful not to assign it to an int value.<br /><br />The <b>blue()</b> function is easy to use and undestand, but is slower than another technique. To achieve the same results when working in <b>colorMode(RGB, 255)</b>, but with greater speed, use a bit mask to remove the other color components. For example, the following two lines of code are equivalent:<br /><pre>float r1 = blue(myColor);<br />float r2 = myColor & 0xFF;</pre>]]></description>

<syntax><![CDATA[
blue(<kbd>color</kbd>)
]]></syntax>

<parameter>
<label>color</label>
<description><![CDATA[any value of the color datatype]]></description>
</parameter>

<returns>float</returns>

<related>
red() 
green() 
hue() 
saturation() 
brightness() 
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>





</root>
