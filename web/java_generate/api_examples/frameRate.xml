<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>frameRate()</name>

<category>Environment</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code><![CDATA[
void setup() {
  frameRate(4);
}
int pos = 0;
void draw() {
  background(204);
  pos++;
  line(pos, 20, pos, 80);
  if(pos &gt; width) {
    pos = 0;
  }
}
]]></code>
</example>

<description><![CDATA[Specifies the number of frames to be displayed every second. If the processor is not fast enough to maintain the specified rate, it will not be achieved. For example, the function call <b>frameRate(30)</b> will attempt to refresh 30 times a second. It is recommended to set the frame rate within <b>setup()</b>. The default rate is 60 frames per second.]]></description>

<syntax><![CDATA[
frameRate(<kbd>fps</kbd>)
]]></syntax>

<parameter>
<label>fps</label>
<description><![CDATA[int: number of frames per second]]></description>
</parameter>

<returns>None</returns>

<related>
delay()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>




</root>
