<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>setup()</name>

<category>Structure</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code><![CDATA[
void setup() {
  size(200, 200);
  background(0);
  noStroke();
  fill(102);
}

int a = 0;

void draw() {
  rect(a++%width, 10, 2, 80); 
}
]]></code>
</example>

<description><![CDATA[Called once when the program is started. Used to define initial enviroment properties such as screen size, background color, loading images, etc. before the <b>draw()</b> begins executing. Variables declared within <b>setup()</b> are not accessible within other functions, including<b>draw()</b>. There can only be one <b>setup()</b> function for each program and it should not be called again after it's initial execution.]]></description>

<syntax><![CDATA[
void setup() {
  <kbd>statements</kbd>
}
]]></syntax>

<parameter>
<label>statements</label>
<description><![CDATA[any valid statements]]></description>
</parameter>

<returns>None</returns>

<related>
loop()
size()
</related>

<availability>1.0</availability>

<type>Processing Function</type>

<partof>PDE</partof>








</root>
