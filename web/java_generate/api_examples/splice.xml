<?xml version="1.0" encoding="UTF-8" standalone="yes"?>

<root>

<name>splice()</name>

<category>Data</category>

<subcategory>Array Functions</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code><![CDATA[
String[] sa1 = { "OH", "NY", "CA" };
sa1 = splice(sa1, "KY", 1);
println(sa1);  // Prints OH, KY, NY, CA
println();

String[] sa2 = { "VA", "CO", "IL" };
sa1 = splice(sa1, sa2, 2);
println(sa1);  // Prints OH, KY, VA, CO, IL, NY, CA
]]></code>
</example>

<description><![CDATA[
Inserts a value or array of values into an existing array. The first two parameters must be of the same datatype. The <b>array</b> parameter defines the array which will be modified and the second parameter defines the data which will be inserted.
<br/> <br/>
When using an array of objects, the data returned from the function must be cast to the object array's data type. For example: <em>SomeClass[] items = (SomeClass[]) splice(array1, array2, index)</em>.
]]></description>

<syntax><![CDATA[
splice(<kbd>array</kbd>, <kbd>value</kbd>, <kbd>index</kbd>)
splice(<kbd>array</kbd>, <kbd>array2</kbd>, <kbd>index</kbd>)
]]></syntax>

<parameter>
<label>array</label>
<description><![CDATA[boolean[], byte[], char[], int[], float[], String[], or an array of objects]]></description>
</parameter>

<parameter>
<label>value</label>
<description><![CDATA[boolean, byte, char, int, float, String, or other Object: value to be spliced in]]></description>
</parameter>

<parameter>
<label>array2</label>
<description><![CDATA[boolean[], byte[], char[], int[], float[], String[], or an array of objects to be spliced in]]></description>
</parameter>

<parameter>
<label>index</label>
<description><![CDATA[int: position in the array from which to insert data]]></description>
</parameter>

<returns>Array (the same datatype as the input)</returns>

<related>
contract()
subset()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>

</root>
