<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>pixels[]</name>

<category>PImage</category>

<subcategory>Field</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>PImage_pixels.jpg</image>
<code><![CDATA[
PImage img = loadImage("tower.jpg");
int dimension = (img.width*img.height);
img.loadPixels();
for (int i=0; i &lt; dimension; i+=2) { 
  img.pixels[i] = color(0, 0, 0); 
} 
img.updatePixels();
image(img, 0, 0);
]]></code>
</example>

<description><![CDATA[Array containing the values for all the pixels in the image. These values are of the color datatype. This array is the size of the image, meaning if the image is 100x100 pixels, there will be 10000 values and if the window is 200x300 pixels, there will be 60000 values. The <b>index</b> value defines the position of a value within the array. For example, the statement <b>color b = img.pixels[230]</b> will set the variable <b>b</b> equal to the value at that location in the array. Before accessing this array, the data must loaded with the <b>loadPixels()</b> method. After the array data has been modified, the <b>updatePixels()</b> method must be run to update the changes. Without <b>loadPixels()</b>, running the code may (or will in future releases) result in a NullPointerException.]]></description>

<syntax><![CDATA[
<kbd>img</kbd>.pixels[<kbd>index</kbd>]
]]></syntax>

<parameter>
<label>img</label>
<description><![CDATA[PImage: any variable of type PImage]]></description>
</parameter>

<parameter>
<label>index</label>
<description><![CDATA[int: must not exceed the size of the array]]></description>
</parameter>

<returns></returns>

<related>
get()
set()
blend()
copy()
</related>

<availability>1.0</availability>

<type>Field</type>

<partof>Core</partof>








</root>
