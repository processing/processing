<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>scale()</name>

<category>Transform</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>scale_.gif</image>
<code><![CDATA[
rect(30, 20, 50, 50);
scale(0.5);
rect(30, 20, 50, 50);
]]></code>
</example>

<example>
<image>scale_2.gif</image>
<code><![CDATA[
rect(30, 20, 50, 50);
scale(0.5, 1.3);
rect(30, 20, 50, 50);
]]></code>
</example>

<example>
<image>scale_3.gif</image>
<code><![CDATA[
// Scaling in 3D requires P3D 
// or OPENGL as a parameter to size()
size(100, 100, P3D);
fill(255, 102);
translate(width/2+12, height/2);
box(20, 20, 20);
scale(2.5, 2.5, 2.5);
box(20, 20, 20);
]]></code>
</example>

<description><![CDATA[Increases or decreases the size of a shape by expanding and contracting vertices. Objects always scale from their relative origin to the coordinate system. Scale values are specified as decimal percentages. For example, the function call <b>scale(2.0)</b> increases the dimension of a shape by 200%. Transformations apply to everything that happens after and subsequent calls to the function multiply the effect. For example, calling <b>scale(2.0)</b> and then <b>scale(1.5)</b> is the same as <b>scale(3.0)</b>. If <b>scale()</b> is called within <b>draw()</b>, the transformation is reset when the loop begins again. Using this fuction with the <b>z</b> parameter requires passing P3D or OPENGL into the size() parameter as shown in the example above. This function can be further controlled by <b>pushMatrix()</b> and <b>popMatrix()</b>.]]></description>

<syntax><![CDATA[
scale(<kbd>size</kbd>);
scale(<kbd>x</kbd>, <kbd>y</kbd>);
scale(<kbd>x</kbd>, <kbd>y</kbd>, <kbd>z</kbd>);
]]></syntax>

<parameter>
<label>size</label>
<description><![CDATA[float: percentage to scale the object]]></description>
</parameter>

<parameter>
<label>x</label>
<description><![CDATA[float: percentage to scale the object in the x-axis]]></description>
</parameter>

<parameter>
<label>y</label>
<description><![CDATA[float: percentage to scale the object in the y-axis]]></description>
</parameter>

<parameter>
<label>z</label>
<description><![CDATA[float: percentage to scale the object in the z-axis]]></description>
</parameter>

<returns>None</returns>

<related>
translate() 
rotate() 
rotateX() 
rotateY() 
rotateZ() 
pushMatrix() 
popMatrix() 
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
