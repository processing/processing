<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>month()</name>

<category>Input</category>

<subcategory>Time &amp; Date</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code><![CDATA[
void setup() {
  PFont metaBold;
  metaBold = loadFont("fonts/Meta-Bold.vlw.gz"); 
  setFont(metaBold, 44); 
  noLoop();
}

void draw() {
  int d = day();    // Values from 1 - 31
  int m = month();  // Values from 1 - 12
  int y = year();   // 2003, 2004, 2005, etc.
  String s = String.valueOf(d);
  text(s, 10, 28);
  s = String.valueOf(m);
  text(s, 10, 56); 
  s = String.valueOf(y);
  text(s, 10, 84);
}
]]></code>
</example>

<description><![CDATA[Processing communicates with the clock on your computer. The <b>month()</b> function returns the current month as a value from 1 - 12.]]></description>

<syntax><![CDATA[
month()
]]></syntax>

<parameter>
<label></label>
<description></description>
</parameter>

<returns>int</returns>

<related>
millis() 
second()
minute()
hour()
day()
month()
year()
</related>

<availability>1.0</availability>

<type>Function</type>

<partof>Core</partof>








</root>
