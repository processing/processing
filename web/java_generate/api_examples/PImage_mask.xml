<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>mask()</name>

<category>PImage</category>

<subcategory>Pixels</subcategory>

<usage>Web &amp; Application</usage>

<example>
<image>mask_.jpg</image>
<code><![CDATA[
PImage img = loadImage("test.jpg");
PImage maskImg = loadImage("mask.jpg");
img.mask(maskImg);
image(img, 0, 0);
image(img, 25, 0);
]]></code>
</example>

<description><![CDATA[Masks part of an image from displaying by loading another image and using it as an alpha channel. This mask image should only contain grayscale data, but only the blue color channel is used. The mask image needs to be the same size as the image to which it is applied.<br /><br />In addition to using a mask image, an integer array containing the alpha channel data can be specified directly. This method is useful for creating dynamically generated alpha masks. This array must be of the same length as the target image's pixels array and should contain only grayscale data of values between 0-255.]]></description>

<syntax><![CDATA[
<kbd>img</kbd>.mask(<kbd>maskImg</kbd>)
<kbd>img</kbd>.mask(<kbd>maskArray</kbd>)
]]></syntax>

<parameter>
<label>img</label>
<description><![CDATA[any variable of type PImage]]></description>
</parameter>
<parameter>

<label>maskImg</label>
<description><![CDATA[PImage: any PImage object used as the alpha channel for "img", needs to be same size as "img"]]></description>
</parameter>
<parameter>

<label>maskArray</label>
<description><![CDATA[int[]: any array of Integer numbers used as the alpha channel, needs to be same length as the image's pixel array]]></description>
</parameter>

<returns></returns>

<related></related>

<availability>1.0</availability>

<type>Method</type>

<partof>Core</partof>


</root>
